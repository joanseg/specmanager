---
id: plan-redesign-001
featureId: feat-redesign
stage: plan
status: approved
stale: false
title: Redesign plan
dependsOn:
  - arch-redesign-001
basedOn:
  arch-redesign-001: 4
generatedBy: agent
version: 7
createdAt: '2026-05-28T12:10:28.503Z'
updatedAt: '2026-06-11T15:01:26.871Z'
---
> Status: draft · awaiting human review and approval in the board UI.
> Based on: Architecture `arch-redesign-001` v4 (approved), which is itself based on PRD `prd-redesign-001` v2 (approved). No SpecManager `design`-stage doc exists for this feature; the visual source-of-truth lives as raw materials under `docs/temp/redesign/` (`DESIGN.md` "Obsidian Flux", reference mockups `board.png` and `panel.png`).

## Overview

This plan executes the **presentation-layer redesign** of SpecManager's own board UI and document panel to the "Obsidian Flux" design system. It is a pure re-skin: no `@specmanager/core`, MCP-tool, board-server API/WS, or gate-semantics changes (confirmed against Architecture §1, §3, §4 and PRD Non-goals). All work lands under `plugins/specmanager/ui/src/` plus a new token + font layer; the `tsc -p && vite build` → `ui/dist` pipeline and the Fastify `@fastify/static` serving path are unchanged.

The work is organised into **three phases**. **Phase A** (shipped) laid the foundation — token layer, fonts, every component re-skinned, BUILD relocated, responsive at three breakpoints. **Phase B** (shipped) was a **design-fidelity pass**: a visual diff of the delivered board against the `board.png` / `panel.png` mockups surfaced that Phase A landed the palette/tokens/layout but missed the *depth* the design system calls for (tonal elevation + glass + micro-interactions) plus a few high-signal details (display type on the header title, the gradient primary CTA, and the focused right-side drawer treatment). Phase B closed exactly those gaps. **Phase C** is a **layout-correctness pass**: a live DOM measurement at 1280px against the mockups found that Phase B added depth/gradient/drawer *styling* but the underlying box model never fit the grid — populated stage cards overflow their grid row and overlap the next feature row, cards in a row are ragged (different heights, content-hugging widths), the Walkthroughs roll-up label clips its card border, and the panel header still stacks vertically instead of being a single horizontal bar. Phase C fixes the geometry only. It is still presentation-only and still gated on the same build + manual-acceptance discipline; it introduces no new product capability, data field, or endpoint, and is mostly box-model/flex/grid (likely no new colors).

## Phase summary

| Phase | Theme | Tasks | Points |
| --- | --- | --- | --- |
| A | Obsidian Flux re-skin of board + doc panel | 12 | 29 |
| B | Design-fidelity pass — depth, display type, gradient CTA, drawer | 6 | 13 |
| C | Layout-correctness pass — grid box-model + horizontal panel header | 5 | 9 |
| D | Board-layout tweaks — drop two dividers + reorder BUILD before WALKTHROUGHS + cap card height | 5 | 9 |

## Phase A — Obsidian Flux re-skin of board + doc panel

**Exit test:** From the repo root run `npm --prefix plugins/specmanager/ui run build` (i.e. `tsc -p && vite build`) — it succeeds with the new font assets + tokens bundled. Install/run the plugin (`claude plugin install ./plugins/specmanager --scope local`, start a session to boot the board server) and open the localhost board. The board renders on a True Dark `#131315` canvas with a glassmorphic top bar ("SpecManager" + feature count + Rescan whose tooltip shows the last-synced time), tonal-elevation cards carrying status badges + mono version labels, and BUILD as a right-hand per-row region. Click a stage card → the doc panel shows the stage/version header with Approve + Edit/Reopen over a raw-source-left / rendered-right split. A manual pass confirms approve/reopen, save + 409-conflict banner, staleness display, gate check, chat, copy-slash helpers, and task editing still work. The board visually matches `docs/temp/redesign/board.png` and the panel matches `docs/temp/redesign/panel.png`; a grep for legacy hex (`#0d0e10`, `#7ad0ff`) finds none outside `tokens.css`. Layout holds with no broken overflow at <640px, 640–1024px, and >1024px.

| # | Task | Pts | Notes |
| --- | --- | --- | --- |
| 1 | Add self-hosted font deps and author `ui/src/fonts.css` | 2 | Add `@fontsource/hanken-grotesk`, `@fontsource/inter`, `@fontsource/geist-mono` (latest 5.2.8) to `ui/package.json`. Author `ui/src/fonts.css` importing only the confirmed weights (Architecture Q8): Hanken Grotesk 500/600, Inter 400/500, Geist Mono 400/500, all `font-display: swap`. Foundation — no consumers yet, so safe to land first. |
| 2 | Author `ui/src/tokens.css` Obsidian Flux token layer | 3 | New `:root` mapping every `docs/temp/redesign/DESIGN.md` frontmatter value to a CSS custom property: surfaces/tonal-elevation, on-surface text, outline/outline-variant, primary/secondary/tertiary/error, radii sm/md/lg = 4/8/16px, 8px spacing scale, tri-font role stacks, `--shadow-overlay`, `--glass-bg`/`--glass-blur`. Include the load-bearing status-color mapping (Architecture §4 table) AND the one derived token with the **pinned hex**: `--success: #10b981` (Architecture Q1 answer — the emerald not present in DESIGN.md frontmatter). |
| 3 | Wire token+font imports, base/reset, and `index.html` head | 1 | Import `tokens.css` then `fonts.css` at the top of `styles.css` (imported once via `main.tsx`); replace the legacy `:root` block + base/reset to consume tokens (`--surface`, `--on-surface`, `--font-body`). In `index.html` add `<meta name="color-scheme" content="dark">` and optional font `<link rel="preload">`. After this, no `:root` color lives outside `tokens.css`. |
| 4 | Re-skin board header to glassmorphic top bar | 2 | `App.tsx` `board__header`: sticky glass top bar (`--glass-bg` + `--glass-blur`, hairline `--outline-variant`), title in `--font-display`, feature count in `--font-body`/mono. Add a **Rescan** button wired to the existing `reload()` (no new endpoint, PRD Q3); fold the footer "Last synced" timestamp into the Rescan button's `title` tooltip and drop the footer (Architecture Q6 answer). |
| 5 | Relocate BUILD out of the grid into a right-hand region | 3 | Per `board.png` + Architecture §6/Q3 (option A): remove `"build"` from the iterated grid `COLUMNS`; render a fixed-width right-hand BUILD track per feature row showing a compact progress glance (progress bar + `done/total`, phases, in-progress, copy-`/specmanager-execute`) that opens the existing `BuildPanel` overlay on click. Keep `BuildCell`'s empty/ready/plan-not-approved states. Preserve row alignment in the same CSS grid (reserved final track). Do not touch the `BuildPanel` overlay behavior. |
| 6 | Re-skin board grid cards and cell sub-components | 3 | Re-style `card`, `card--button`, `card--stale`, `card--empty`/`--locked`/`--optional`, badges (`badge--draft/approved/stale/meta/design`), and the walkthrough sub-cards (`card--walkthroughs`, `card--sub`, `card--final`) to tonal-elevation surfaces: `--surface-container` family, `--radius-md`, 1px `--outline-variant` border that brightens/violet-tints on hover/active, no drop shadow. Version labels (`v1`) in `--font-mono` Geist. Keep DESIGN column (PRD Q2). Re-skin the no-features `empty` block and `state`/`state--error`. |
| 7 | Re-skin DocPanel header, actions, banners, split, stale section | 3 | `DocPanel.tsx`: stage/version header (stage label + `v{version}` mono + status badge) matching `panel.png`, with Approve on draft and **Edit** on approved mapping to existing `onReopen` (draft transition; no new semantics). Restyle toolbar, the Preview/Chat toggles, the floating panel (`--shadow-overlay`, `--radius-lg` leading edge), the stale `panel__stale`/drift section, and the warn/error/ok banners. Default split stays raw-left/rendered-right (`showPreview` true). Keep the design-brief sandboxed iframe (white bg) untouched; don't let dark `.markdown` styles bleed into it. Verify 409-conflict + saved banners stay legible. |
| 8 | Re-skin BuildPanel phase groups, progress bar, pills, artifacts | 3 | `BuildPanel.tsx`: re-skin phase groups (`phase-group`, `--done`), the `.bar` progress bar (`--success` done seg / `--tertiary` in-progress seg), status pills as glowing-dot active chips (`status-pill--on` per status mapped to success/tertiary/primary tokens), and artifact inputs with focus glow. Behavior/layout unchanged; remains the click-to-open overlay. |
| 9 | Re-skin ChatPanel to the new palette | 2 | `ChatPanel.tsx` styles: message rows (`msg--user/assistant/info/tool/error`) to token surfaces, assistant accent border + `msg__caret` to `--primary`, composer textarea focus glow, header chips. Behavior unchanged. |
| 10 | Re-theme the CodeMirror editor to Obsidian Flux tokens | 2 | `Editor.tsx`: swap the inline `EditorView.theme` to read tokens — `.cm-scroller` Geist mono (`--font-mono`), gutter/active-line/selection colors from `--outline-variant`/`--surface-container`, primary focus glow. Keep `{ dark: true }` and the read-only compartment; ensure selection/active-line contrast on `#131315` stays legible. |
| 11 | Responsive pass + reduced-motion guard | 3 | Add `styles.css` media queries for the three Obsidian Flux breakpoints (Architecture §6): desktop >1024px (full grid + right-hand BUILD + wide slide-in), tablet 640–1024px (narrow columns, BUILD wraps/collapses to compact strip, panel drops chat then collapses), mobile <640px (grid linearizes to stacked cards 16px margin, panels full-width, split collapses to single tabbed column). CSS-only, no JS layout logic. Add `@media (prefers-reduced-motion: reduce)` to disable scale/glow transitions (Architecture Q9). Provide a `@supports not (backdrop-filter: blur(1px))` opaque glass fallback. |
| 12 | Build, behavior, and visual verification pass | 2 | Run `npm --prefix plugins/specmanager/ui run build` (tsc + vite) and confirm it succeeds and the bundle still serves from the board server. Grep component blocks for surviving legacy hex (`#0d0e10`, `#7ad0ff`) → none outside `tokens.css`. Manual behavior pass (approve/reopen, save + conflict, staleness, gate, chat, copy-slash, task editing). Visual compare against `docs/temp/redesign/board.png` + `panel.png` at all three breakpoints. |

## Phase B — Design-fidelity pass: depth, display type, gradient CTA, drawer

Phase B is additive polish on the **already-shipped** Phase A surface. It closes the four high-signal gaps a visual diff (delivered screenshots `docs/temp/redesign/Board as is.png` / `Panel as is.png` vs mockups `board.png` / `panel.png`) surfaced, plus one lower-priority detail. It stays presentation-only: every change lands in `tokens.css`, `styles.css`, `App.tsx`, and `DocPanel.tsx`, and any new color is added to `tokens.css` so the legacy-hex grep gate stays clean. The foundation-first discipline from Phase A repeats: the depth/elevation tokens (B1) land before the card rules (B2) that consume them, and the gradient token (B3) lands before the CTA that reads it.

**Exit test:** From the repo root `npm --prefix plugins/specmanager/ui run build` (`tsc -p && vite build`) succeeds; the legacy-hex grep (`#0d0e10`, `#7ad0ff`, and any one-off color) finds nothing outside `tokens.css` — all Phase B colors/gradients/shadows are tokens. Install/run the plugin and open the board: cards (board stage cards + walkthrough sub-cards) now show tonal-elevation depth with a brightened/violet-tinted border and a hover glow + slight scale (matching `board.png`), and the "SpecManager" header title renders in the large Hanken Grotesk display face. Click a stage card → the doc panel opens as a focused right-side **drawer (~65% width)** with the dimmed board still visible behind it, a large soft shadow, and a glass leading edge (matching `panel.png`); the **Approve** action is a filled indigo→purple gradient primary button while Save/Edit/Gate read as secondary/ghost; and the rendered pane carries the stage breadcrumb header. Reduced-motion still disables the new glow/scale. Layout holds at <640px (drawer goes full-width), 640–1024px, and >1024px. A manual pass confirms approve/reopen, save + 409-conflict banner, staleness, gate, chat, and task editing still work unchanged.

| # | Task | Pts | Notes |
| --- | --- | --- | --- |
| 1 | Add depth/elevation tokens to `tokens.css` (card gradient, brightened border, hover glow) | 1 | Foundation for B2/B4 — land first so no consumer references a raw hex. In `tokens.css` add: `--card-gradient` (a subtle 135° `--surface-container` → `--surface-container-low` fill for "depth through layering"), `--card-border` and `--card-border-hover` (hairline that brightens / violet-tints toward `--primary` on hover, per DESIGN.md Cards "border transitions to a subtle gradient or brighter neutral"), `--shadow-card` (soft tonal glow for hover, distinct from the existing modal `--shadow-overlay`), and `--glass-edge` (semi-transparent leading-edge tint for the drawer). All derived from existing palette tokens via `color-mix` — no new raw hex except where a literal rgba is unavoidable (kept in `tokens.css` only). |
| 2 | Apply card depth + hover glow/scale to board cards and walkthrough sub-cards | 2 | In `styles.css` restyle `.card` (179) and `.card--button` (322): swap the flat `--surface-container` fill for `--card-gradient`, the border to `--card-border`; on `:hover` use `--card-border-hover`, add `box-shadow: var(--shadow-card)`, and a slight `transform: scale(1.01)` / `translateY(-1px)` micro-interaction (DESIGN.md "subtle glows and slight scale shifts indicate interactivity"). Mirror the depth on walkthrough sub-cards (`.card--sub` / `.card--final` at 982/1002) at a lighter weight. Leave `--empty`/`--locked`/`--optional` dashed affordances and `.card--stale` border-color logic intact. Extend the existing `@media (prefers-reduced-motion: reduce)` block (1079) so the new scale + glow transition are disabled. |
| 3 | Add primary gradient CTA token + establish button hierarchy | 2 | In `tokens.css` add `--gradient-primary` (135° indigo→purple from `--primary-container`/`--secondary-container` per DESIGN.md Buttons §Primary) and `--gradient-primary-hover`. In `styles.css` rewrite `.btn--primary` (487) to a filled gradient button: gradient background, `--on-primary`-derived light text, a 1px top-border highlight (`inset 0 1px 0` on `--primary`), and a brightened hover. Keep `.btn` (secondary: surface + 1px border) and `.btn--ghost` as-is so the three-tier hierarchy (primary/secondary/ghost) is explicit. CSS-only; no JSX touched here. |
| 4 | Re-point DocPanel actions to the new button hierarchy | 1 | In `DocPanel.tsx`: the **Approve** button (currently bare `.btn` at ~224) becomes the gradient primary (`.btn btn--primary`) — Approve is the high-intent action per DESIGN.md. The **Save** button (currently `.btn btn--primary` at ~216) drops to secondary `.btn` (it is conditional/transient). Leave **Edit** as secondary `.btn` and **Gate?** as `.btn--ghost`. No behavior, handler, or disabled-logic changes — class swaps only; verify the dirty/readOnly disabled states still gate correctly. |
| 5 | Constrain DocPanel to a ~65%-width drawer over the dimmed board | 3 | In `styles.css` change `.panel` (362) width from `min(80rem, 95vw)` to a focused drawer `min(52rem, 65vw)` so the dimmed board stays visible behind it (matching `panel.png`); keep `--shadow-overlay` (already large/soft) and add the `--glass-edge` leading-edge treatment (glass tint + `--glass-blur` on the left border zone). Verify the `.panel-backdrop` (354) dim still reads as "board behind". Confirm the cols-1/2/3 `panel__body` split still fits at 65% (chat-open = 3 cols is the tightest); if cramped, allow chat to drop first via the existing tablet breakpoint logic — do NOT change behavior. Preserve the mobile full-width override (`.panel { width: 100vw }` at ~1070). |
| 6 | Add rendered-pane stage breadcrumb header + Phase B verification pass | 2 | Lower-priority mockup detail + the phase gate. In `DocPanel.tsx` add a small stage breadcrumb above the title in `panel__header` (e.g. `<nav className="panel__crumb">{featureTitle?} › {STAGE_LABEL[doc.stage]}</nav>`) styled in `styles.css` with `--font-mono`/`--on-surface-variant` to match `panel.png`; derive the crumb from data already on `doc` (no new fetch/endpoint). Then run the Phase B exit test: `npm --prefix plugins/specmanager/ui run build` green; legacy-hex grep clean (new gradients/shadows live only in `tokens.css`); visual re-check of card depth + hover glow, display-type header, gradient Approve, and the 65% drawer against `board.png`/`panel.png` at all three breakpoints; reduced-motion check; behavior regression pass (approve/reopen, save + conflict, staleness, gate, chat, task editing). |

> **Note on display type (gap 2):** the header-title gap is closed inside Phase B's CSS without its own row — `.board__title` (styles.css:71) is bumped from `1.5rem` to the `display-lg` Hanken Grotesk scale as part of B2's styles work, since `App.tsx:396` already applies the `.board__title` class and no JSX change is required. The B6 visual check explicitly verifies it.

## Phase C — Layout-correctness pass: grid box-model + horizontal panel header

Phase C is a **geometry-only** correction on the already-shipped Phase A/B surface. Phase B added depth, gradient, and the drawer treatment but a live DOM re-measure at **1280px** against the mockups found that the box model never fit the grid: populated stage cards render taller than their grid row track and spill into the next feature row; cards in one row are ragged (different heights and content-hugging widths instead of uniform full-width equal-height); the Walkthroughs "★ Feature roll-up" sub-label overlaps its card's top border; and the panel header is still a vertical stack rather than the single horizontal bar the mockup shows. Every fix lands in `styles.css`, `App.tsx`, and `DocPanel.tsx` under `plugins/specmanager/ui/` — same envelope as A/B. It is box-model/flex/grid work only: **no behavior changes** (all Save/Edit/Approve/Gate/Preview/Chat handlers, the 409/conflict/staleness/gate/chat/task-editing flows, the design-brief iframe, and the Phase B button hierarchy are preserved). It is almost certainly colorless; if any rgba/shadow is unavoidable it lives only in `tokens.css` so the legacy-hex grep gate stays clean. Foundation-first ordering holds: the grid/cell box-model fix (C1) lands before the sub-card label fix (C2) that sits inside it, and the panel-header regroup (C3) lands before the breadcrumb/drawer polish (C4) that rides on it; the verification pass (C5) runs last.

**Exit test:** From the repo root `npm --prefix plugins/specmanager/ui run build` (`tsc -p && vite build`) succeeds; the legacy-hex grep (`#0d0e10`, `#7ad0ff`, and any one-off color) finds nothing outside `tokens.css`. Install/run the plugin and open the board, then re-measure the live DOM at **1280px** and confirm: **(a)** no stage card's bottom crosses into the next feature row — a row's height is driven by its tallest cell and there is no row-to-row overlap; **(b)** all cards in a single row are equal height and bottom-aligned; **(c)** cards fill their column width uniformly (no content-hugging / left-aligned narrow cards); **(d)** the Walkthroughs "★ Feature roll-up" label sits fully inside its card (no border overlap); **(e)** the doc panel header is a single horizontal bar with the document title + stage breadcrumb on the left and the Save/Approve/Edit/Gate actions aligned right on the **same** row, matching `board.png`/`panel.png`; and the breadcrumb shows the human feature **title** ("Redesign"), not the raw `feat-redesign` id, with the drawer at the ~65% target. Reduced-motion behavior is unchanged. Layout holds at <640px, 640–1024px, and >1024px. A behavior-regression pass confirms approve/reopen, save + 409-conflict banner, staleness, gate, chat, and task editing still work unchanged.

| # | Task | Pts | Notes |
| --- | --- | --- | --- |
| 1 | Fix grid row box-model: content-sized row tracks + full-width equal-height bottom-aligned cards | 2 | Fixes board defects 1–3. In `App.tsx` the grid uses `display:contents` rows, so row height is driven by the implicit row track. In `styles.css`: make grid cells stretch and cards fill them — `.row__cell` / `.row__cell--build` already get `align-items: stretch` from `.grid { align-items: stretch }` (verify), and `.card` / `.card--button` already declare `height: 100%` (179/186, 343) but render at intrinsic height because each `.row__cell` has only `min-height: 5.5rem` (177) with no stretch on the cell's own content box. Set `.row__cell` to `display: flex` so its single child card stretches to the full row-track height (`.card { flex: 1 }` or `height: 100%` against a flex parent), making every card in a row equal-height and bottom-aligned (badges already pin to the bottom via `.card__badges { margin-top: auto }` at 274). Confirm cards fill column **width** (`.card`/`.card--button` are flex-column already; ensure they stretch horizontally — the `.row__cell` flex child should be `align-self: stretch`/`width: 100%`). Net effect at 1280px: a row's height equals its tallest cell, no card overflows into the next feature row, and PRD/ARCH/PLAN/BUILD cards are uniform full-column-width. No JSX change required; box-model only. |
| 2 | Fix Walkthroughs roll-up sub-label overlapping its card top border | 1 | Fixes board defect 4. In `styles.css` the `.card--sub` padding (1023: `0.42rem 0.55rem`) and `.card__sub-label` (1052) let the "★ Feature roll-up" label render against/over the card's top border in the tight Walkthroughs stack. Nudge the sub-card top padding / label box so the label sits inside the border (e.g. ensure the label's line-box clears the 1px border and the `--radius-sm` corner). Touch only `.card--sub` / `.card__sub-label` / `.card--final` label spacing — leave the depth (`--card-gradient`/`--card-border`) and the dashed empty/locked variants intact. Verify the per-phase sub-cards (which use the same classes) still read correctly. |
| 3 | Regroup the panel header into a single horizontal bar (title/crumb left, actions right) | 3 | Fixes panel defect 5. Today `.panel__header` (402) is `display:block` so the close ×, breadcrumb, title-row, and badges stack on separate lines, and the Save/Approve/Edit/Gate buttons live in a **separate** `.panel__toolbar` row below (217–263). Lay the header out as a flex row: `.panel__header { display:flex; justify-content:space-between; align-items:center; gap }` with a left cluster (breadcrumb + title) and a right cluster holding the primary actions. In `DocPanel.tsx`, move the Save / Approve-or-Edit / Gate? buttons up into the header's right cluster (keep the Preview/Chat toggles where they belong — header right or the existing toolbar strip, whichever matches `panel.png`). **Preserve every handler and disabled-logic** (`onSave`/`onApprove`/`onReopen`/`onShowGate`, the `dirty`/`readOnly` gating) and the Phase B button hierarchy (Approve = `btn btn--primary` gradient, Save/Edit = `btn`, Gate? = `btn--ghost`) — this is a layout regroup, not a behavior change. Keep the `.panel__close` × accessible (absolute or in the bar). Banners (conflict/error/saved) and the body split stay below the header unchanged. Re-check the header reflow inside the ~65% drawer and at the <640 breakpoint (header may wrap to two rows on mobile — acceptable). |
| 4 | Breadcrumb shows feature title + nudge drawer to the ~65% target | 1 | Two minor Phase-B-review polish items. (1) Breadcrumb fix: `DocPanel.tsx` (176) renders `doc.featureId` (raw `feat-redesign`); show the human feature **title** instead. `DocFull` carries no `title` for the feature, so derive a display label from `featureId` (strip the `feat-` prefix and title-case, e.g. `feat-redesign` → "Redesign") in a one-line helper — no new fetch/endpoint. (2) Drawer width: the live measure put the drawer at ~57% vs the ~65% target; in `styles.css` `.panel` (384) is `min(52rem, 65vw)` — the `52rem` cap is what bites at 1280px, so nudge the cap (e.g. `min(60rem, 65vw)` or drop to `65vw`) so the drawer reads at ~65%, **only if** it doesn't fight C3's header layout or the cols-3 split. Preserve the mobile full-width override. |
| 5 | Phase C verification pass | 2 | The phase gate. Run `npm --prefix plugins/specmanager/ui run build` (tsc + vite) → green. Legacy-hex grep (`#0d0e10`, `#7ad0ff`, any one-off color) → nothing outside `tokens.css`. Live re-measure at **1280px** and confirm exit-test items (a)–(e): no stage card bottom crosses into the next feature row, all cards in a row equal-height + bottom-aligned, cards fill column width uniformly, the Walkthroughs roll-up label sits inside its card, and the panel header is a single horizontal bar (title/crumb left, actions right) with the human feature title in the breadcrumb and the drawer at ~65%. Reduced-motion unchanged. Behavior-regression pass (approve/reopen, save + 409 conflict, staleness, gate, chat, task editing). Layout holds at <640 / 640–1024 / >1024px. |

## Phase D — Board-layout tweaks: drop two dividers + reorder BUILD before WALKTHROUGHS + cap card height

Phase D is four small **presentation-only** board-layout tweaks on the already-shipped Phase A/B/C surface, same envelope as before: every change lands in `styles.css` and `App.tsx` under `plugins/specmanager/ui/` (no `DocPanel.tsx` this time), and touches no `@specmanager/core`, MCP tool, board-server, manifest, or gate semantics. It is almost certainly colorless — the changes are border removals, a track/render reorder, and a card-height cap (a dimension); if any rgba/shadow were unavoidable it would live only in `tokens.css` so the legacy-hex grep gate stays clean. **No behavior changes**: the BUILD cell's content (the compact progress glance), its click-to-open `BuildPanel` overlay, and every Phase A/B/C flow are preserved — only BUILD's column **position** moves and the cards gain a height cap. Two of the tweaks are isolated single-rule border removals (D1, D2) and are independent; D3 is the only structural one, touching both files plus the responsive breakpoints, so it lands as its own task after the divider removals; D4 caps the stage-card height so a sparse row no longer inflates the Phase-C equal-height cards (a box-model tweak independent of the reorder); the verification pass (D5) runs last.

**Exit test:** From the repo root `npm --prefix plugins/specmanager/ui run build` (`tsc -p && vite build`) succeeds; the legacy-hex grep (`#0d0e10`, `#7ad0ff`, and any one-off color) finds nothing outside `tokens.css`. Install/run the plugin and open the board, then re-check the live DOM at **1280px** and confirm: **(a)** there is **no underline / bottom-border divider beneath the column titles** (FEATURE · PRD · ARCHITECTURE · DESIGN · PLAN · BUILD · WALKTHROUGHS); **(b)** there is **no vertical divider between the FEATURE (row-label) column and the PRD column** — only the existing grid gap separates them; **(c)** the column order reads **FEATURE · PRD · ARCHITECTURE · DESIGN · PLAN · BUILD · WALKTHROUGHS** with the BUILD cell intact (same compact glance + overlay) and aligned in its row; and **(d)** the populated stage cards render at the **capped, mock-like height** (not stretched to fill the row track) matching `docs/temp/redesign/board.png` — a sparse single-feature row no longer inflates the cards to ~206–226px — **while the Phase C invariants still hold**: cards in a row are still equal-height *up to the cap* and bottom-aligned (badges pinned bottom), fill their column width uniformly, and there is no row-to-row overlap; normal card content (title + badges + version) fits inside the cap with no inner scroll, matching the mock. The single horizontal panel header still holds. Reduced-motion behavior is unchanged. Layout holds at <640px, 640–1024px, and >1024px (verify the responsive breakpoints still hold after the BUILD reorder and the card cap — the tablet `grid-column: 1 / -1` BUILD strip and the mobile linearized stack). Behavior unchanged.

| # | Task | Pts | Notes |
| --- | --- | --- | --- |
| 1 | Remove the divider below each column title | 1 | Tweak 1. In `styles.css` the column-header row draws a hairline under each title: `.grid__corner, .grid__header` carries `border-bottom: 1px solid var(--outline-variant)` (151), and `.grid__header--build` overrides that border to `--primary` (153–156). Remove the `border-bottom` from `.grid__corner, .grid__header` and drop the now-orphaned `border-bottom-color: var(--primary)` from `.grid__header--build` (keep its `color: var(--primary)` label tint). After this no underline sits beneath any column title. CSS-only; no JSX. Leave the header padding so the titles keep their spacing above the cards. |
| 2 | Remove the vertical divider between the FEATURE and PRD columns | 1 | Tweak 2. In `styles.css` `.row__label` (the first / FEATURE row-label column) carries `border-right: 1px solid var(--outline-variant)` (163) — the vertical separator between the FEATURE column and PRD. Remove that `border-right`; the existing grid `gap: var(--space-sm)` (140) is the only separation that remains, matching the other column gaps. Leave every other column boundary untouched. Note the mobile <640px block already resets this to a `border-bottom` for the stacked layout (`.row__label { border-right: 0; border-bottom: … }` at ~1173) — leave that mobile rule as-is so the linearized stack keeps its separator. CSS-only; no JSX. |
| 3 | Reorder the BUILD column to sit after PLAN and before WALKTHROUGHS | 3 | Tweak 3 — the only structural change, touches **both** files. Today BUILD is the rightmost track: in `App.tsx` the header `grid__header--build` and the `row__cell--build` are rendered **after** the `STAGES.map(...)` loop (lines ~428–447), and the grid track string `--grid-cols` puts the wide BUILD track last (`12rem repeat(${STAGES.length}, minmax(11rem, 1fr)) 14rem` at ~423). `STAGES` is `["prd","architecture","design","plan","walkthrough"]` (types.ts:5), so BUILD must be inserted **between `plan` and `walkthrough`** in render + track order. Approach (layout-only, no `types.ts`/`COLUMNS` semantic change): in `App.tsx` render the stage cells by splitting the map so the BUILD header + `row__cell--build` are emitted right after the `plan` column and before `walkthrough` (e.g. iterate stages and inject the BUILD cell at the `plan`/`walkthrough` boundary, or render `prd…plan`, then BUILD, then `walkthrough`), keeping the exact same `BuildCell`/`Cell column="build"` markup and the `.row__cell--build` class. Move the wide `14rem` BUILD track in the `--grid-cols` string to the matching position (after the 4 `prd…plan` tracks, before the `walkthrough` track) — i.e. switch from a single `repeat(5, …)` to explicit tracks so BUILD sits 6th: `12rem repeat(4, minmax(11rem,1fr)) 14rem minmax(11rem,1fr)`. Then in `styles.css` verify the responsive breakpoints still hold: the tablet `@media (max-width:1024px)` rule sets `--grid-cols: 10rem repeat(5, minmax(8rem,1fr))` and spans `.row__cell--build { grid-column: 1 / -1 }` (1126–1134) — `1 / -1` still spans the full row regardless of source order, but confirm the BUILD strip still wraps below the row correctly; and the mobile `<640px` linearized single-column stack (1166–1177) still stacks BUILD in DOM order (now between PLAN and WALKTHROUGHS — acceptable). Preserve the BUILD cell content and the click-to-open `BuildPanel` overlay exactly. |
| 4 | Cap board card height to match the mock (sparse rows no longer inflate cards) | 2 | Tweak 4 — presentation-only, same envelope (only `styles.css`; touch `App.tsx` *only if* the grid row track must change). Fixes the Phase C side effect: making `.row__cell` a flex parent with `.card { flex: 1; height: auto }` (styles.css 180–195, set by C/task-019 & task-023) makes cards stretch to the full row-track height, so with only one populated feature row the track grows and the stage cards balloon to ~206–226px (measured live at 1280px) versus the compact title+badges cards in `docs/temp/redesign/board.png`. Read `board.png` to fix the target height. Add a `max-height` on the stage cards (`.card` ~179 / `.card--button` ~199) sized to the mock so a sparse row stops inflating them — **preserving the Phase C wins**: cards in a row stay equal-height *up to the cap* and bottom-aligned (badges already pin bottom via `.card__badges { margin-top: auto }`), uniform full-column width, and no row-to-row overlap. If capping the card means the row track itself should be content/cap-driven rather than stretching, adjust the `.row__cell` / `.grid` row-track rule (and the `App.tsx` `--grid-cols`/row sizing only if unavoidable) so the track follows the cap — without reintroducing the C overlap or ragged-height regressions. Prefer the mock's behavior for overflow: the mock cards are compact (title + badges, no inner scroll), so choose the cap so normal card content (title + badges + `v{n}` version label) fits; only if a populated card would exceed the cap, keep it readable rather than clipping critical content. Colorless — any new value is a dimension; no new colors (legacy-hex grep stays clean). Leave the dashed `card--empty`/`--locked`/`--optional` affordances, `card--stale`, and the `card--walkthroughs` sub-card stack intact. |
| 5 | Phase D verification pass | 2 | The phase gate. Run `npm --prefix plugins/specmanager/ui run build` (tsc + vite) → green. Legacy-hex grep (`#0d0e10`, `#7ad0ff`, any one-off color) → nothing outside `tokens.css`. Live re-check at **1280px** and confirm exit-test items (a)–(d): no underline beneath the column titles, no vertical divider between FEATURE and PRD, the column order reads FEATURE · PRD · ARCHITECTURE · DESIGN · PLAN · BUILD · WALKTHROUGHS with the BUILD cell intact (compact glance + overlay) and aligned, and the populated stage cards now render at the **capped, mock-like height** (not stretched to fill the row) — a sparse single-feature row no longer inflates them — while still equal-height + bottom-aligned within a row, uniform full-column width, with no row-to-row overlap (Phase C invariants hold); plus the single horizontal panel header still holds. Reduced-motion unchanged. Responsive holds at <640 / 640–1024 / >1024px (tablet BUILD strip + mobile linearized stack). Behavior-regression pass (open a stage doc, open the BUILD overlay, approve/reopen, save + 409 conflict, staleness, gate, chat, task editing) — unchanged. |

## Risk & sequencing notes

- **Foundation must land first.** Tasks 1→2→3 are strictly ordered: fonts and tokens have to exist before any component task can reference `--font-*` or `--surface-*`. Everything from task 4 onward depends on task 3 (the import wiring + base reset).
- **Pinned emerald is load-bearing (Architecture §9 / Q1).** Obsidian Flux's DESIGN.md frontmatter has **no** success/done color — only `tertiary` (amber) and `error` (rose). The board needs a distinct approved/done color, so task 2 introduces a single derived token `--success: #10b981` (the exact hex confirmed in the approved architecture). Both BuildPanel done segments/pills (task 8) and the approved badge (task 6) must consume this token, not a one-off hex — otherwise the legacy-hex grep gate in task 12 fails.
- **BUILD relocation is the only structural change (task 5).** It is option A (Architecture Q3): the right-hand region is a read-only progress glance; the full `BuildPanel` overlay behavior is preserved. If the region grows complex it MAY be extracted to `ui/src/BuildRail.tsx`, but that is not assumed and not a separate task. Rollback is contained to `App.tsx` grid layout — revert `COLUMNS` to include `"build"` to restore the old column.
- **Behavior-preservation hot spots.** The optimistic-concurrency banners (409 conflict / error / saved) and the sandboxed design-brief iframe are the things most likely to be silently broken by a color-only pass. Task 7 explicitly verifies the banners and protects the iframe; task 12 re-verifies end-to-end.
- **CodeMirror can drift from the rest of the UI (task 10).** The editor theme is set inline in JS, so it must read the same CSS custom properties as the stylesheet or selection/active-line colors will diverge on True Dark.
- **What blocks what (Phase A):** 1→2→3 → {4, 6} → 5 (relocation depends on the re-skinned header/cards being in place) → 7 (panel) → {8, 9} → 10 → 11 (responsive depends on the final desktop layout existing) → 12 (verification last).
- **Phase B is additive and low-blast-radius.** It never touches `@specmanager/core`, the MCP tools, the board server, or gate semantics — same envelope as Phase A. Each change is a token addition or a class/value swap on the existing Phase-A CSS, so rollback is a per-task revert of the relevant `tokens.css` / `styles.css` / `*.tsx` hunk.
- **What blocks what (Phase B):** B1 (depth/elevation tokens) → B2 (card depth + display type) and B5 (drawer glass edge), since both consume the new tokens; B3 (gradient token + `.btn--primary` rewrite) → B4 (DocPanel re-points Approve/Save to the new classes); B6 (breadcrumb + verification) runs last after every other Phase B change is in place. B2/B3/B5 are otherwise independent once B1 lands.
- **Drawer width vs the 3-column split (B5).** Narrowing the panel to ~65% is the tightest constraint when chat is open (cols-3). The fix is layout-only and must reuse the existing tablet collapse behavior (chat drops first) — do not introduce new JS layout logic or change which panes show.
- **Gradient contrast (B3).** The indigo→purple primary fill must keep legible light text and an accessible focus ring on True Dark; verify the `--on-primary`-derived text contrast in B4's manual pass alongside the disabled (dirty/readOnly) states.
- **Phase C is geometry-only and the lowest blast-radius yet.** It changes box-model/flex/grid values plus one JSX regroup (the header) and one one-line breadcrumb helper. It touches no tokens (likely), no `@specmanager/core`, no MCP/board-server, no gate semantics. Rollback is a per-task revert of the relevant `styles.css` / `DocPanel.tsx` / `App.tsx` hunk.
- **Header regroup is the one with hidden coupling (C3).** Moving the action buttons from `.panel__toolbar` into `.panel__header` must keep the Save/Approve/Edit/Gate handlers and their `dirty`/`readOnly` disabled gating wired exactly as-is, and keep the Phase B button hierarchy (gradient Approve, secondary Save/Edit, ghost Gate). The conflict/error/saved banners and the cols-1/2/3 body split must stay below the header. Re-check the header reflow inside the 65% drawer and at <640px (a two-row wrap on mobile is acceptable).
- **Grid stretch can regress the dashed empty/locked cells (C1).** Making cells stretch and cards `flex:1` must not break the centered dashed `card--empty`/`card--locked`/`card--optional` affordances or the `.card--stale` border. Verify those variants and the Walkthroughs container (`card--walkthroughs`, which is a transparent borderless stack) still lay out correctly after the cell becomes a flex parent.
- **Drawer width nudge vs the cols-3 split (C4).** Widening the `52rem` cap toward ~65% must not crowd the chat-open 3-column body; reuse the existing tablet collapse rather than new JS, and confirm it doesn't fight the new horizontal header from C3.
- **What blocks what (Phase C):** C1 (grid/cell box-model) → C2 (sub-card label fix sits inside the corrected cells); C3 (header regroup) → C4 (breadcrumb + drawer polish ride on the new header); C5 (verification) runs last after C1–C4. C1/C2 and C3/C4 are otherwise independent tracks (board vs panel).
- **Phase D is the lowest blast-radius pass — two border removals, one column reorder, and a card-height cap.** It touches only `styles.css` and `App.tsx` (no `DocPanel.tsx`), no tokens (likely; the card cap is a dimension, no new color), no `@specmanager/core`, no MCP/board-server, no gate semantics. Rollback is a per-task revert of the relevant `styles.css` / `App.tsx` hunk.
- **The BUILD reorder is the one structural change (D3).** It is a render-order + grid-track move, not a semantic one: `STAGES`/`COLUMNS` in `types.ts` are left intact, so the gate logic (`priorStageApproved`) and the `Cell column="build"` markup are unchanged — only where the BUILD cell is emitted in the row and where its `14rem` track sits in `--grid-cols` move. The grid-track string switches from `repeat(5, …)` to explicit tracks so the wide BUILD track lands 6th (between PLAN and WALKTHROUGHS). Rollback is reverting the `App.tsx` render split + the `--grid-cols` string back to the trailing BUILD layout.
- **Responsive is the D3 hot spot.** The tablet `@media (max-width:1024px)` BUILD strip spans `grid-column: 1 / -1` (full row, order-independent) and the mobile `<640px` linearized stack renders cells in DOM source order — both must be re-verified after the reorder so the tablet strip still wraps below the row and the mobile stack reads PRD…PLAN, BUILD, WALKTHROUGHS without overflow. D5 explicitly re-checks all three breakpoints.
- **What blocks what (Phase D):** D1 (column-title underline) and D2 (FEATURE↔PRD vertical divider) are independent single-rule border removals; D3 (BUILD reorder, both files + responsive) lands after them to keep the structural change isolated; D4 (cap card height) is an independent box-model tweak on the Phase C flex/stretch cells and can land any time after the dividers (it does not depend on the BUILD reorder); D5 (verification) runs last after D1–D4.
- **D4 must not undo Phase C (the card-height cap).** The cap rides directly on the Phase C box-model (`.row__cell` flex + `.card { flex: 1; height: auto }`). Capping height must keep cards equal-height *up to the cap* and bottom-aligned, full-column-width, and must not reintroduce the C row-to-row overlap or ragged heights; if the cap requires making the grid row track content/cap-driven rather than stretching, that change is contained to `styles.css` (and `App.tsx` `--grid-cols`/row sizing only if unavoidable). Choose the cap so normal card content (title + badges + version) fits with no inner scroll, per the mock. Rollback is a single `styles.css` (± `App.tsx`) hunk revert.

## Test strategy

The UI package has **no automated test runner** (verified: `ui/package.json` has only `dev`/`build`/`preview`; no test deps or config). Per the repo's existing convention, the gate is the **build + manual acceptance pass** rather than unit tests:
- **Per task:** keep the build green — re-run `npm --prefix plugins/specmanager/ui run build` after component tasks so `tsc` strict (`noUncheckedIndexedAccess`) and `vite build` stay clean.
- **Phase A final pass (task 12):** the full behavior + visual + breakpoint + legacy-hex-grep verification, mapping 1:1 to the PRD acceptance criteria.
- **Phase B final pass (task B6):** the same build + legacy-hex-grep + visual-diff (now against the *depth/display/gradient/drawer* fidelity targets) + reduced-motion + behavior-regression verification.
- **Phase C final pass (task C5):** build + legacy-hex-grep + a **live DOM re-measure at 1280px** (the box-model defects are only catchable by measuring rendered geometry, not by visual diff alone) verifying the five exit-test items, plus the reduced-motion check, the three-breakpoint check, and the behavior-regression pass. This remains the single source of "done" for the phase, consistent with how the rest of SpecManager's UI work has been validated (no test harness introduced by a presentation-only feature).
- **Phase D final pass (task D5):** build + legacy-hex-grep + a **live re-check at 1280px** confirming the four layout tweaks (no column-title underline; no FEATURE↔PRD vertical divider; column order FEATURE · PRD · ARCHITECTURE · DESIGN · PLAN · BUILD · WALKTHROUGHS with the BUILD cell intact; and the populated stage cards capped at the mock-like height rather than stretched to fill a sparse row) and that the Phase C correctness still holds (equal-height-up-to-cap + bottom-aligned, full-column width, no row overlap), plus the reduced-motion check, the three-breakpoint check (with special attention to the tablet BUILD strip and the mobile linearized stack after the reorder), and the behavior-regression pass. The card-height cap is verifiable only by a **live DOM re-measure** (the cards measuring ~206–226px is the defect; the cap brings them to the mock-like compact height). Same build + manual-acceptance gate; no test harness introduced.

## Out of scope

- **No `@specmanager/core`, manifest, staleness-graph, or gate-semantics changes** (Architecture §3, PRD Non-goals). Data model is byte-for-byte identical.
- **No MCP-tool or board-server HTTP/WS contract changes** (Architecture §4). No new endpoints — "Rescan" reuses the existing client `reload()`; the Phase B breadcrumb derives from already-loaded doc data with no new fetch.
- **No new product capabilities** — no new stages, editing modes, or data fields.
- **Canonical `docs/DESIGN.md` is NOT modified** (PRD Q1 answer: keep separate, reconcile later via re-sync). The styling source for this feature is `docs/temp/redesign/DESIGN.md` only.
- **No framework swap** — stays React 18 + Vite + single global stylesheet + CodeMirror 6 + `marked`; no CSS-in-JS.
- **DESIGN column is kept** despite `board.png` omitting it (PRD Q2) — Phase B/C do **not** remove it. The mockup's omission is intentional per the PRD, not a fidelity gap.
- **The compact BUILD glance stays as-is** (Phase A option-A decision) — Phase B/C do not redesign the BUILD rail; Phase C only makes its cell obey the same equal-height grid box-model as the other cards.
- **No `BuildRail.tsx` extraction** unless the right-hand region proves complex (flagged, not assumed; not a task).
- **Phase C adds no behavior, color, or content** — it is box-model/flex/grid plus one header JSX regroup and one breadcrumb-label helper. No new product surface, no token changes assumed.
- **Phase D adds no behavior, color, or content** — it removes two dividers, moves the BUILD column between PLAN and WALKTHROUGHS, and caps the stage-card height to the mock (a dimension, no new color). The DESIGN column is **kept** (PRD Q2). The BUILD cell's compact glance and its click-to-open overlay are unchanged; `STAGES`/`COLUMNS` semantics and the gate logic are untouched (the reorder is render-order + grid-track only). The card cap does **not** redesign the cards or reintroduce drop shadows/per-card scroll — it only stops a sparse row from inflating equal-height cards past the mock. No token changes assumed; only `styles.css` and `App.tsx` are touched.

## Open questions

None blocking. The Architecture (v4) already resolved every flagged risk inline: success emerald pinned to `#10b981` (Q1); BUILD = option A compact glance + existing overlay (Q3); DESIGN column kept (Q4); footer timestamp → Rescan tooltip (Q6); font weights Hanken Grotesk 500/600, Inter 400/500, Geist Mono 400/500 (Q8); reduced-motion guard on (Q9). No design-stage doc exists, so there is no draft-design contradiction to flag. Phase C introduces no new questions — it grounds entirely in the live-measured Phase-A/B code and the same `board.png`/`panel.png` mockups; the only judgement calls (exact sub-card label padding, the drawer-cap value, where the Preview/Chat toggles land in the regrouped header) are bounded by the mockups and resolved in-task. One small grounding note carried into C4: `DocFull` exposes only `featureId`, not a feature title, so the breadcrumb derives its human label from the id (strip `feat-`, title-case) rather than fetching one. Phase D introduces no new questions — it grounds entirely in the live code (`App.tsx` grid render + `styles.css` column-header/`row__label`/`row__cell--build`/`.row__cell`/`.card` rules) and the four explicitly-specified tweaks; the judgement calls (whether the BUILD reorder uses a render split or an injected-cell map; the exact card-height cap value read off `board.png`, and whether capping also makes the grid row track content/cap-driven) are bounded and resolved in-task (D3, D4), and `STAGES`/`COLUMNS` semantics are deliberately left untouched.
